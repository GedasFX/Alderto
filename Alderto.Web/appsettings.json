{
  /**
   * Password of the application, do not worry, you will not have to ever remember this.
   * Set to anything you like, Minimum required length - 32.
   * Allowed characters: 'A-Z a-z 0-9 / +'
   */
  "JWTPrivateKey": "",

  /**
   * PostgreSQL authentication.
   */
  "Database": {
    "Host": "localhost",
    "Port": "5432",
    "Database": "Alderto",
    "Username": "postgres",
    "Password": ""
  },

  /**
   * Discord application keys.
   * https://discordapp.com/developers/applications/
   */
  "DiscordAPI": {
    "ClientId": "", /* https://discordapp.com/developers/applications/[Your_Bot_Application_ID]/information */
    "ClientSecret": "", /* https://discordapp.com/developers/applications/[Your_Bot_Application_ID]/information */
    "BotToken": "" /* https://discordapp.com/developers/applications/[Your_Bot_Application_ID]/bots */
  },
  "Discord": {
    "AdminServerId": "", /* Id of server considered the master for the bot. */
    "ErrorLogChannelId": "", /* Id of channel in master server, where to send error logs. */
    "NewsChannelId": "" /* Id of channel in master server, where to get news from to display on front page. */
  },

  /* Advanced. */
  /**
   * Logging customization configuration.
   * Log levels: Trace, Debug, Information, Warning, Error, Critical.
   * For help see https://docs.microsoft.com/en-us/aspnet/core/fundamentals/logging/?view=aspnetcore-2.2#configuration
   */
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },

  /**
   * Kestrel configuration.
   * For help see https://docs.microsoft.com/en-us/aspnet/core/fundamentals/servers/kestrel?view=aspnetcore-2.2#endpoint-configuration
   */
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://*"
      },

      /* If you do not wish to use SSL, remove the part "Https" entirely. */
      "Https": {
        "Url": "https://*",

        /* Optional: fill this information only if system does not automatically find a valid certificate (doesn't start up). */
        "Certificate": {
          "Path": "",
          "Password": ""
        }
      }
    }
  }
}
